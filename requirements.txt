# Core Flask Framework
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.3
Flask-Admin==1.6.1
# Flask-Admin 1.6.1 emits 3-tuple form choices; WTForms >=3.1 expects 4-tuples
# and raises "not enough values to unpack" on Select fields. Pin to the last 3.0.x.
WTForms==3.0.1
Flask-Migrate==4.0.5
Werkzeug==2.3.7

# HTTP Requests and API
requests==2.31.0
websocket-client==1.6.4

# Environment and Configuration
python-dotenv==1.0.0

# # Machine Learning and Data Processing
numpy>=1.26.0
scikit-learn>=1.3.2  # required to unpickle the saved_models/*.pkl disease models
# pandas>=2.1.1

# # LangChain - These versions have better support for PEP 517 (modern builds)
# langchain>=0.1.0
# langchain-community>=0.0.20
# sentence-transformers==2.2.2

# # PDF Processing
# PyPDF2==3.0.1

# # Jupyter Notebook Support (for research files)
# jupyter==1.0.0
# ipykernel==6.25.0
# notebook==7.0.0

# # Additional ML Libraries (commonly used in healthcare projects)
# matplotlib==3.7.2
# seaborn==0.12.2

# Development and Testing
pytest==7.4.0
pytest-flask==1.2.0

# Production Server
gunicorn==21.2.0

# PostgreSQL driver (used when DATABASE_URL is set, e.g. Render Postgres, so
# data persists across deploys; SQLite is used automatically when it is unset)
psycopg2-binary==2.9.9