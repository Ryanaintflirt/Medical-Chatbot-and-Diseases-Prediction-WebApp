# Dependencies for building the RAG index OFFLINE (build_rag_index.py).
# These are NOT needed to run the web app — the deployed runtime only needs
# numpy + requests (already in requirements.txt) to load the pre-built index.
# Install with:  pip install -r requirements-rag.txt
langchain-community>=0.2
langchain-text-splitters>=0.2
pypdf>=4.0
