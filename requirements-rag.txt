# Dependencies for (re)building the Pinecone 'medicalbot' index OFFLINE via the
# research notebook (research/trials.ipynb). NOT needed to run the web app —
# the deployed runtime uses fastembed (in requirements.txt) to query the index.
langchain-community>=0.2
langchain-text-splitters>=0.2
langchain-pinecone>=0.1
pinecone>=5.0
sentence-transformers>=2.2
pypdf>=4.0
