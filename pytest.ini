[pytest]
testpaths = tests
python_files = test_*.py
# Third-party libraries still call datetime.utcnow(); silence that deprecation
# noise so the suite output stays focused on real results.
filterwarnings =
    ignore::DeprecationWarning
